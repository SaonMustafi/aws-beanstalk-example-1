version: 0.2
phases:
  pre_build:
    commands:
    - echo "Entered the pre-build phase"
    build:
      commands:
      - echo "Entered Build phase"
      - mvn install
  post_build:
      commands:
      - echo "Entered Post build"
artifacts:
    files:
      - aws-elastic-beanstalk-example-1.war